spring:
  hateoas:
    use-hal-as-default-json-media-type: false
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2
  data:
    web:
      pageable:
        max-page-size: 500
        default-page-size: 10


storage:
  type: memory
